import { NextResponse } from 'next/server';

// TODO: POST → transition colleague from draft to accepted
export async function POST() {
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 });
}
