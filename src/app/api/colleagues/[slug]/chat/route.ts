import { NextResponse } from 'next/server';

// TODO: POST → chat with the colleague agent (streaming)
export async function POST() {
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 });
}
